# Publish-only Pages deploy. This never scrapes (scraping happens locally, from a
# residential IP, because the source blocks datacenter IPs). It just takes the
# committed data/listings.json plus the static site/ and publishes them.

name: Deploy site to Pages

on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - "data/listings.json"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Copy data into the site
        run: cp data/listings.json site/listings.json

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - id: deployment
        uses: actions/deploy-pages@v4
